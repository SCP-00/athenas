{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "athenas:index",
  "title": "Index",
  "description": "Athenas Project Index — authority Level 8",
  "type": "object",
  "required": ["id", "title", "date", "status", "purpose"],
  "properties": {
    "id": { "type": "string", "pattern": "^INDEX-\\d{4}$" },
    "title": { "type": "string" },
    "date": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
    "status": {
      "type": "string",
      "enum": ["Active", "Stale", "Archived"]
    },
    "purpose": { "type": "string" }
  }
}
